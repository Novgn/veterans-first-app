/**
 * Shared hooks for Veterans First application
 *
 * Note: These hooks are designed to be used in both React Native and Next.js.
 * Platform-specific hooks should be implemented in their respective apps.
 */

// Re-export hooks as they are created
// Example: export { useDebounce } from "./useDebounce";

// Placeholder export to prevent empty module
export const HOOKS_VERSION = "0.0.0";
